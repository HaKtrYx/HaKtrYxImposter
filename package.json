{
  "name": "imposter-game",
  "version": "1.0.0",
  "private": true,
  "description": "Imposter Game - Full Stack Application",
  "scripts": {
    "build": "cd client && npm run build && cd ../server && npm run copy:client",
    "start": "cd server && npm start",
    "dev": "concurrently \"cd client && npm run dev\" \"cd server && npm run dev\"",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}